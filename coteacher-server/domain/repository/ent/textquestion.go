// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/KinjiKawaguchi/Coteacher/coteacher-server/domain/repository/ent/question"
	"github.com/KinjiKawaguchi/Coteacher/coteacher-server/domain/repository/ent/textquestion"
	"github.com/google/uuid"
)

// TextQuestion is the model entity for the TextQuestion schema.
type TextQuestion struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// QuestionID holds the value of the "question_id" field.
	QuestionID uuid.UUID `json:"question_id,omitempty"`
	// MaxLength holds the value of the "max_length" field.
	MaxLength int `json:"max_length,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TextQuestionQuery when eager-loading is set.
	Edges        TextQuestionEdges `json:"edges"`
	selectValues sql.SelectValues
}

// TextQuestionEdges holds the relations/edges for other nodes in the graph.
type TextQuestionEdges struct {
	// Question holds the value of the question edge.
	Question *Question `json:"question,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// QuestionOrErr returns the Question value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TextQuestionEdges) QuestionOrErr() (*Question, error) {
	if e.loadedTypes[0] {
		if e.Question == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: question.Label}
		}
		return e.Question, nil
	}
	return nil, &NotLoadedError{edge: "question"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TextQuestion) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case textquestion.FieldMaxLength:
			values[i] = new(sql.NullInt64)
		case textquestion.FieldID, textquestion.FieldQuestionID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TextQuestion fields.
func (tq *TextQuestion) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case textquestion.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				tq.ID = *value
			}
		case textquestion.FieldQuestionID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field question_id", values[i])
			} else if value != nil {
				tq.QuestionID = *value
			}
		case textquestion.FieldMaxLength:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field max_length", values[i])
			} else if value.Valid {
				tq.MaxLength = int(value.Int64)
			}
		default:
			tq.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TextQuestion.
// This includes values selected through modifiers, order, etc.
func (tq *TextQuestion) Value(name string) (ent.Value, error) {
	return tq.selectValues.Get(name)
}

// QueryQuestion queries the "question" edge of the TextQuestion entity.
func (tq *TextQuestion) QueryQuestion() *QuestionQuery {
	return NewTextQuestionClient(tq.config).QueryQuestion(tq)
}

// Update returns a builder for updating this TextQuestion.
// Note that you need to call TextQuestion.Unwrap() before calling this method if this TextQuestion
// was returned from a transaction, and the transaction was committed or rolled back.
func (tq *TextQuestion) Update() *TextQuestionUpdateOne {
	return NewTextQuestionClient(tq.config).UpdateOne(tq)
}

// Unwrap unwraps the TextQuestion entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (tq *TextQuestion) Unwrap() *TextQuestion {
	_tx, ok := tq.config.driver.(*txDriver)
	if !ok {
		panic("ent: TextQuestion is not a transactional entity")
	}
	tq.config.driver = _tx.drv
	return tq
}

// String implements the fmt.Stringer.
func (tq *TextQuestion) String() string {
	var builder strings.Builder
	builder.WriteString("TextQuestion(")
	builder.WriteString(fmt.Sprintf("id=%v, ", tq.ID))
	builder.WriteString("question_id=")
	builder.WriteString(fmt.Sprintf("%v", tq.QuestionID))
	builder.WriteString(", ")
	builder.WriteString("max_length=")
	builder.WriteString(fmt.Sprintf("%v", tq.MaxLength))
	builder.WriteByte(')')
	return builder.String()
}

// TextQuestions is a parsable slice of TextQuestion.
type TextQuestions []*TextQuestion
