// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/KinjiKawaguchi/Coteacher/coteacher-server/domain/repository/ent/class"
	"github.com/KinjiKawaguchi/Coteacher/coteacher-server/domain/repository/ent/predicate"
	"github.com/KinjiKawaguchi/Coteacher/coteacher-server/domain/repository/ent/student"
	"github.com/KinjiKawaguchi/Coteacher/coteacher-server/domain/repository/ent/studentclass"
	"github.com/google/uuid"
)

// StudentClassQuery is the builder for querying StudentClass entities.
type StudentClassQuery struct {
	config
	ctx         *QueryContext
	order       []studentclass.OrderOption
	inters      []Interceptor
	predicates  []predicate.StudentClass
	withStudent *StudentQuery
	withClass   *ClassQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the StudentClassQuery builder.
func (scq *StudentClassQuery) Where(ps ...predicate.StudentClass) *StudentClassQuery {
	scq.predicates = append(scq.predicates, ps...)
	return scq
}

// Limit the number of records to be returned by this query.
func (scq *StudentClassQuery) Limit(limit int) *StudentClassQuery {
	scq.ctx.Limit = &limit
	return scq
}

// Offset to start from.
func (scq *StudentClassQuery) Offset(offset int) *StudentClassQuery {
	scq.ctx.Offset = &offset
	return scq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (scq *StudentClassQuery) Unique(unique bool) *StudentClassQuery {
	scq.ctx.Unique = &unique
	return scq
}

// Order specifies how the records should be ordered.
func (scq *StudentClassQuery) Order(o ...studentclass.OrderOption) *StudentClassQuery {
	scq.order = append(scq.order, o...)
	return scq
}

// QueryStudent chains the current query on the "student" edge.
func (scq *StudentClassQuery) QueryStudent() *StudentQuery {
	query := (&StudentClient{config: scq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := scq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := scq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(studentclass.Table, studentclass.FieldID, selector),
			sqlgraph.To(student.Table, student.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, studentclass.StudentTable, studentclass.StudentColumn),
		)
		fromU = sqlgraph.SetNeighbors(scq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryClass chains the current query on the "class" edge.
func (scq *StudentClassQuery) QueryClass() *ClassQuery {
	query := (&ClassClient{config: scq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := scq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := scq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(studentclass.Table, studentclass.FieldID, selector),
			sqlgraph.To(class.Table, class.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, studentclass.ClassTable, studentclass.ClassColumn),
		)
		fromU = sqlgraph.SetNeighbors(scq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first StudentClass entity from the query.
// Returns a *NotFoundError when no StudentClass was found.
func (scq *StudentClassQuery) First(ctx context.Context) (*StudentClass, error) {
	nodes, err := scq.Limit(1).All(setContextOp(ctx, scq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{studentclass.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (scq *StudentClassQuery) FirstX(ctx context.Context) *StudentClass {
	node, err := scq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first StudentClass ID from the query.
// Returns a *NotFoundError when no StudentClass ID was found.
func (scq *StudentClassQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = scq.Limit(1).IDs(setContextOp(ctx, scq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{studentclass.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (scq *StudentClassQuery) FirstIDX(ctx context.Context) int {
	id, err := scq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single StudentClass entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one StudentClass entity is found.
// Returns a *NotFoundError when no StudentClass entities are found.
func (scq *StudentClassQuery) Only(ctx context.Context) (*StudentClass, error) {
	nodes, err := scq.Limit(2).All(setContextOp(ctx, scq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{studentclass.Label}
	default:
		return nil, &NotSingularError{studentclass.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (scq *StudentClassQuery) OnlyX(ctx context.Context) *StudentClass {
	node, err := scq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only StudentClass ID in the query.
// Returns a *NotSingularError when more than one StudentClass ID is found.
// Returns a *NotFoundError when no entities are found.
func (scq *StudentClassQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = scq.Limit(2).IDs(setContextOp(ctx, scq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{studentclass.Label}
	default:
		err = &NotSingularError{studentclass.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (scq *StudentClassQuery) OnlyIDX(ctx context.Context) int {
	id, err := scq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of StudentClasses.
func (scq *StudentClassQuery) All(ctx context.Context) ([]*StudentClass, error) {
	ctx = setContextOp(ctx, scq.ctx, "All")
	if err := scq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*StudentClass, *StudentClassQuery]()
	return withInterceptors[[]*StudentClass](ctx, scq, qr, scq.inters)
}

// AllX is like All, but panics if an error occurs.
func (scq *StudentClassQuery) AllX(ctx context.Context) []*StudentClass {
	nodes, err := scq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of StudentClass IDs.
func (scq *StudentClassQuery) IDs(ctx context.Context) (ids []int, err error) {
	if scq.ctx.Unique == nil && scq.path != nil {
		scq.Unique(true)
	}
	ctx = setContextOp(ctx, scq.ctx, "IDs")
	if err = scq.Select(studentclass.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (scq *StudentClassQuery) IDsX(ctx context.Context) []int {
	ids, err := scq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (scq *StudentClassQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, scq.ctx, "Count")
	if err := scq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, scq, querierCount[*StudentClassQuery](), scq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (scq *StudentClassQuery) CountX(ctx context.Context) int {
	count, err := scq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (scq *StudentClassQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, scq.ctx, "Exist")
	switch _, err := scq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (scq *StudentClassQuery) ExistX(ctx context.Context) bool {
	exist, err := scq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the StudentClassQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (scq *StudentClassQuery) Clone() *StudentClassQuery {
	if scq == nil {
		return nil
	}
	return &StudentClassQuery{
		config:      scq.config,
		ctx:         scq.ctx.Clone(),
		order:       append([]studentclass.OrderOption{}, scq.order...),
		inters:      append([]Interceptor{}, scq.inters...),
		predicates:  append([]predicate.StudentClass{}, scq.predicates...),
		withStudent: scq.withStudent.Clone(),
		withClass:   scq.withClass.Clone(),
		// clone intermediate query.
		sql:  scq.sql.Clone(),
		path: scq.path,
	}
}

// WithStudent tells the query-builder to eager-load the nodes that are connected to
// the "student" edge. The optional arguments are used to configure the query builder of the edge.
func (scq *StudentClassQuery) WithStudent(opts ...func(*StudentQuery)) *StudentClassQuery {
	query := (&StudentClient{config: scq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	scq.withStudent = query
	return scq
}

// WithClass tells the query-builder to eager-load the nodes that are connected to
// the "class" edge. The optional arguments are used to configure the query builder of the edge.
func (scq *StudentClassQuery) WithClass(opts ...func(*ClassQuery)) *StudentClassQuery {
	query := (&ClassClient{config: scq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	scq.withClass = query
	return scq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		StudentID uuid.UUID `json:"student_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.StudentClass.Query().
//		GroupBy(studentclass.FieldStudentID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (scq *StudentClassQuery) GroupBy(field string, fields ...string) *StudentClassGroupBy {
	scq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &StudentClassGroupBy{build: scq}
	grbuild.flds = &scq.ctx.Fields
	grbuild.label = studentclass.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		StudentID uuid.UUID `json:"student_id,omitempty"`
//	}
//
//	client.StudentClass.Query().
//		Select(studentclass.FieldStudentID).
//		Scan(ctx, &v)
func (scq *StudentClassQuery) Select(fields ...string) *StudentClassSelect {
	scq.ctx.Fields = append(scq.ctx.Fields, fields...)
	sbuild := &StudentClassSelect{StudentClassQuery: scq}
	sbuild.label = studentclass.Label
	sbuild.flds, sbuild.scan = &scq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a StudentClassSelect configured with the given aggregations.
func (scq *StudentClassQuery) Aggregate(fns ...AggregateFunc) *StudentClassSelect {
	return scq.Select().Aggregate(fns...)
}

func (scq *StudentClassQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range scq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, scq); err != nil {
				return err
			}
		}
	}
	for _, f := range scq.ctx.Fields {
		if !studentclass.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if scq.path != nil {
		prev, err := scq.path(ctx)
		if err != nil {
			return err
		}
		scq.sql = prev
	}
	return nil
}

func (scq *StudentClassQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*StudentClass, error) {
	var (
		nodes       = []*StudentClass{}
		_spec       = scq.querySpec()
		loadedTypes = [2]bool{
			scq.withStudent != nil,
			scq.withClass != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*StudentClass).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &StudentClass{config: scq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, scq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := scq.withStudent; query != nil {
		if err := scq.loadStudent(ctx, query, nodes, nil,
			func(n *StudentClass, e *Student) { n.Edges.Student = e }); err != nil {
			return nil, err
		}
	}
	if query := scq.withClass; query != nil {
		if err := scq.loadClass(ctx, query, nodes, nil,
			func(n *StudentClass, e *Class) { n.Edges.Class = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (scq *StudentClassQuery) loadStudent(ctx context.Context, query *StudentQuery, nodes []*StudentClass, init func(*StudentClass), assign func(*StudentClass, *Student)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*StudentClass)
	for i := range nodes {
		fk := nodes[i].StudentID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(student.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "student_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (scq *StudentClassQuery) loadClass(ctx context.Context, query *ClassQuery, nodes []*StudentClass, init func(*StudentClass), assign func(*StudentClass, *Class)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*StudentClass)
	for i := range nodes {
		fk := nodes[i].ClassID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(class.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "class_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (scq *StudentClassQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := scq.querySpec()
	_spec.Node.Columns = scq.ctx.Fields
	if len(scq.ctx.Fields) > 0 {
		_spec.Unique = scq.ctx.Unique != nil && *scq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, scq.driver, _spec)
}

func (scq *StudentClassQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(studentclass.Table, studentclass.Columns, sqlgraph.NewFieldSpec(studentclass.FieldID, field.TypeInt))
	_spec.From = scq.sql
	if unique := scq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if scq.path != nil {
		_spec.Unique = true
	}
	if fields := scq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, studentclass.FieldID)
		for i := range fields {
			if fields[i] != studentclass.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if scq.withStudent != nil {
			_spec.Node.AddColumnOnce(studentclass.FieldStudentID)
		}
		if scq.withClass != nil {
			_spec.Node.AddColumnOnce(studentclass.FieldClassID)
		}
	}
	if ps := scq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := scq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := scq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := scq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (scq *StudentClassQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(scq.driver.Dialect())
	t1 := builder.Table(studentclass.Table)
	columns := scq.ctx.Fields
	if len(columns) == 0 {
		columns = studentclass.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if scq.sql != nil {
		selector = scq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if scq.ctx.Unique != nil && *scq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range scq.predicates {
		p(selector)
	}
	for _, p := range scq.order {
		p(selector)
	}
	if offset := scq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := scq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// StudentClassGroupBy is the group-by builder for StudentClass entities.
type StudentClassGroupBy struct {
	selector
	build *StudentClassQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (scgb *StudentClassGroupBy) Aggregate(fns ...AggregateFunc) *StudentClassGroupBy {
	scgb.fns = append(scgb.fns, fns...)
	return scgb
}

// Scan applies the selector query and scans the result into the given value.
func (scgb *StudentClassGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, scgb.build.ctx, "GroupBy")
	if err := scgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StudentClassQuery, *StudentClassGroupBy](ctx, scgb.build, scgb, scgb.build.inters, v)
}

func (scgb *StudentClassGroupBy) sqlScan(ctx context.Context, root *StudentClassQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(scgb.fns))
	for _, fn := range scgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*scgb.flds)+len(scgb.fns))
		for _, f := range *scgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*scgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := scgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// StudentClassSelect is the builder for selecting fields of StudentClass entities.
type StudentClassSelect struct {
	*StudentClassQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (scs *StudentClassSelect) Aggregate(fns ...AggregateFunc) *StudentClassSelect {
	scs.fns = append(scs.fns, fns...)
	return scs
}

// Scan applies the selector query and scans the result into the given value.
func (scs *StudentClassSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, scs.ctx, "Select")
	if err := scs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StudentClassQuery, *StudentClassSelect](ctx, scs.StudentClassQuery, scs, scs.inters, v)
}

func (scs *StudentClassSelect) sqlScan(ctx context.Context, root *StudentClassQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(scs.fns))
	for _, fn := range scs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*scs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := scs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
